{
  "deploy": {
    "subdomain": ".withkoji.com",
    "frontend": {
      "output": "dist",
      "commands": [
        "npm install",
        "export NODE_ENV=production && npm run build"
      ]
    }
  }
}
